1. Создаем Executor newFixedThreadPool, делаем для четырех потоков execute, тем самым запуская 4 потока. А потом ждем их в цикле на get.
При этом получается, что можем застрять на самом длинном потоке, а остальные будут висеть и ждать своего get? Показать код.

